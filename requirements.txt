# Python dependencies cho Linux System Manager
# Cài qua apt (không phải pip): sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# Các module dưới đây đều là built-in hoặc cài qua apt:
# - PyGObject (python3-gi)
# - threading (built-in)
# - subprocess (built-in)
# - ctypes (built-in)
PyGObject
